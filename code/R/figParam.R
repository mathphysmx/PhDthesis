# # to use par() enable the package knitr ang global.par = TRUE in the chunk globalOptions
# par("pch" = 20, # for scatterplots with less than 10e6 vales
#     "cex" = 2,
#     "col" = "steelblue"
#     )
# Paper size: landscape (presentation, widescreen), portrait (A4, letter)
col1 <- "steelblue"
scp <- "darkblue" # points of scatterplot
pch20 <- 20
magn <- 1.5 # magnification
SlidesMag14 <- 1 # Magnification  for figures of  1/4 of a slide
magnification <-  1
